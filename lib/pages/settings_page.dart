import 'package:flutter/material.dart';

class SttingsPage extends StatelessWidget {
  const SttingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Settings Page'),),
    );
  }
}
